<p id="notice"><%= notice %></p>
<!-- 
<p>
  <strong>Name:</strong>
  <%= @post.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @post.description %>
</p>

<p>
  <strong>Category:</strong>
  <%= @post.category_id %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag @post.image.url(:medium) %>
</p> -->

<div class="col-lg-12 col-sm-6 center-show">
    <div class="show-box transform" id="button-transform">
        <!-- <img src="img/portfolio/thumbnails/2.jpg" class="img-responsive" alt=""> -->
        <%= image_tag @post.image.url, class: "img-responsive transform" %>
        <div class="show-box-caption">
            <div class="show-box-caption-content">
                <div class="project-category text-faded">
                    <%= @post.description %>
                </div>
                <div class="project-name">
                    <%= @post.name %>
                </div>
                <div class="project-name">
                    <%= @post.category.name %>
                </div>
            </div>
        </div>
    </div>
    <div class="show-button">
		<%= link_to 'Edit', edit_post_path(@post), class: "btn btn-primary" %> |
		<%= link_to 'Back', portfolio_path, class: "btn btn-primary" %>
	</div>
</div>
